<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

    <style>
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes slideIn {
            from { transform: translateY(100px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        body {
            background: linear-gradient(135deg, #1D2671 0%, #05103A 100%);
            color: white;
            font-family: 'Arial', sans-serif;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            animation: fadeIn 1s ease-in-out;
        }

        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            animation: slideIn 1s ease-in-out;
        }

        .textTitle {
            font-size: 45px;
            font-weight: 900;
        }

        .formulir {
            width: 33%;
            margin-top: 4%;
            display: flex;
            flex-direction: column;
        }

        .text1 {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 5px;
        }

        input {
            background-color: rgba(255, 255, 255, 0.1);
            height: 6vh;
            border-radius: 10px;
            border: none;
            color: white;
            font-size: 16px;
            font-weight: bold;
            padding: 0 1vh;
            margin-bottom: 15px;
            transition: background-color 0.3s ease-in-out;
        }

        input:focus {
            background-color: rgba(255, 255, 255, 0.2);
            outline: none;
        }

        .tombol {
            background-color: #FEA923;
            border: none;
            width: 100%;
            height: 6.5vh;
            border-radius: 12px;
            margin-top: 2vh;
            color: white;
            font-weight: bold;
            font-size: 20px;
            transition: background-color 0.3s ease-in-out, transform 0.3s;
        }

        .tombol:hover {
            background-color: #d98b1e;
            transform: scale(1.05);
        }

        .text-center a {
            color: #FEA923;
            text-decoration: none;
        }

        .text-center a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    
    <div class="container">

        <h1 class="textTitle mb-3">Getting Started</h1>

        <form class="formulir" action="/register" method="POST">
            <label class="text1 me-auto">Full Name</label>
            <input type="text" name="fullname" id="fullname" class="mb-3" required>
            
            <label class="text1 me-auto">Phone Number</label>
            <input type="text" name="phonenumber" id="phonenumber" class="mb-3" required>

            <label class="text1 me-auto">Email</label>
            <input type="text" name="email" id="email" class="mb-3" required>

            <label class="text1 me-auto">Password</label>
            <input type="password" name="password" id="password" class="mb-3" required>

            <button type="submit" class="tombol">Register</button>
            <p class="text1 text-center mt-2 fw-medium">Already have an account? <a href="/login">Login</a></p>
        </form>

    </div>

</body>
</html>